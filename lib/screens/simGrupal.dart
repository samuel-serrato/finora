import 'package:flutter/material.dart';

class simuladorGrupal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Simulador Individual'), // Reemplaza esto con tu código
      ),
    );
  }
}
